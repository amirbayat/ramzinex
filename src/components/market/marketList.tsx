import React from "react";
import { TMarketList } from "../../types/market";

type Props = {
  markets: TMarketList | undefined;
};

const MarketList = (props: Props) => {
  const { markets } = props;

  return null;
};

export default MarketList;
